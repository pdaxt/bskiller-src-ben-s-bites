Do you store my API keys?Deploy-time keys are used once, then discarded. Any sensitive data we do store (e.g. in the vault) is encrypted with AES-256-GCM at rest — we never keep plaintext, unlike most hosting providers.What about channel tokens?Same approach. WhatsApp, Telegram, or other tokens are never stored in plaintext. When we hold them, they're encrypted.How do you compare to other providers?Many hostings store API keys and tokens in plaintext or weak encryption. We encrypt every sensitive key at rest and never log or expose plaintext. Your credentials are protected like nowhere else.